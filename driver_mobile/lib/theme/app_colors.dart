import 'package:flutter/material.dart';

/// Centralized color palette constants for the Fleet Driver Mobile application.
class AppColors {
  AppColors._();

  // Primary (Fleet Navy)
  static const Color primary = Color(0xFF0D1B2A);

  // Primary Variant (Deep Navy)
  static const Color primaryVariant = Color(0xFF13293D);

  // Secondary (Fleet Orange)
  static const Color secondary = Color(0xFFFF6A00);

  // Accent (Sunset Orange)
  static const Color accent = Color(0xFFFF8C24);

  // Background
  static const Color background = Color(0xFFFFFFFF);

  // Surface
  static const Color surface = Color(0xFFF7F9FC);

  // Divider
  static const Color divider = Color(0xFFE4E8EF);

  // Primary Text
  static const Color textPrimary = Color(0xFF1B2430);

  // Secondary Text
  static const Color textSecondary = Color(0xFF667085);

  // Disabled Text
  static const Color textDisabled = Color(0xFF98A2B3);

  // Success
  static const Color success = Color(0xFF22C55E);

  // Warning
  static const Color warning = Color(0xFFF59E0B);

  // Error
  static const Color error = Color(0xFFEF4444);

  // Info
  static const Color info = Color(0xFF3B82F6);
}
